# AutoGLM 项目架构对比：Termux 版 vs Chaquopy 版

## 📋 目录
- [核心概念](#核心概念)
- [架构对比](#架构对比)
- [技术选型对比](#技术选型对比)
- [用户体验对比](#用户体验对比)
- [开发维护对比](#开发维护对比)
- [适用场景](#适用场景)

---

## 核心概念

### 什么是 AutoGLM？
AutoGLM 是一个基于大语言模型的**手机自动化助手**。它能理解你的自然语言指令（比如"打开淘宝搜索蓝牙耳机"），然后自动操作手机完成任务。

### 核心挑战
要实现手机自动化，需要解决两个核心问题：
1. **AI 大脑**：如何让 AI 看懂屏幕、做出决策？（需要 Python + AI 模型）
2. **执行手臂**：如何让手机执行点击、滑动等操作？（需要 Android 无障碍服务）

**问题在于**：AI 代码是 Python 写的，但 Android 原生是 Java/Kotlin。两者如何协作？

这就引出了两种不同的解决方案。

---

## 架构对比

### 方案 A：AutoGLM-Hybrid (Termux 版)

#### 架构图
```
┌─────────────────────────────────────────┐
│           Android 手机                   │
│                                          │
│  ┌──────────────┐    ┌───────────────┐  │
│  │   Termux     │    │ AutoGLM Helper│  │
│  │   (大脑)     │◄──►│    (手臂)     │  │
│  │              │HTTP │               │  │
│  │ ┌──────────┐ │8080│ ┌───────────┐ │  │
│  │ │ Python   │ │    │ │无障碍服务 │ │  │
│  │ │ 环境     │ │    │ │           │ │  │
│  │ │          │ │    │ │ 点击/截图 │ │  │
│  │ │ AI 逻辑  │ │    │ │           │ │  │
│  │ └──────────┘ │    │ └───────────┘ │  │
│  └──────────────┘    └───────────────┘  │
│         ▲                    ▲           │
│         │                    │           │
│    用户敲命令           用户开权限        │
└─────────────────────────────────────────┘
```

#### 工作流程
1. 用户安装两个 App：**Termux** + **AutoGLM Helper APK**
2. 在 Termux 里配置 Python 环境（敲命令行）
3. 在 AutoGLM Helper 里开启无障碍权限
4. 在 Termux 里运行脚本：`python agent_main.py`
5. Python 通过 HTTP 请求控制 Helper App
6. Helper App 执行实际的点击、截图操作

#### 类比
**就像遥控玩具车**：
- **Termux** = 遥控器（独立设备，需要电池）
- **Helper APK** = 玩具车（接收指令，执行动作）
- **HTTP** = 无线信号（两者通信）

---

### 方案 B：Open-AutoGLM-Hybrid-main (Chaquopy 版)

#### 架构图
```
┌─────────────────────────────────────────┐
│           Android 手机                   │
│                                          │
│  ┌──────────────────────────────────┐   │
│  │   AutoGLM Helper (All-in-One)   │   │
│  │                                  │   │
│  │  ┌────────────┐  ┌────────────┐ │   │
│  │  │  Python    │  │  Kotlin    │ │   │
│  │  │  (大脑)    │◄─┤  (手臂)    │ │   │
│  │  │            │  │            │ │   │
│  │  │ AI 逻辑    │  │ 无障碍服务 │ │   │
│  │  │ (嵌入式)   │  │            │ │   │
│  │  │            │  │ 点击/截图  │ │   │
│  │  └────────────┘  └────────────┘ │   │
│  │         ▲              ▲         │   │
│  │         └──────────────┘         │   │
│  │        同一个 APK 内部            │   │
│  └──────────────────────────────────┘   │
│                  ▲                       │
│                  │                       │
│            用户点击按钮                   │
└─────────────────────────────────────────┘
```

#### 工作流程
1. 用户安装**一个 APK**（Python 已打包在内）
2. 打开 App，开启无障碍权限
3. 输入任务，点击"执行"
4. Python 代码在 App 内部运行
5. 通过内部调用执行点击、截图

#### 类比
**就像智能自动驾驶车**：
- **整个 APK** = 一辆完整的车（大脑和引擎都在车里）
- **Python 引擎** = 焊死在车里的发动机（用户看不见）
- **Kotlin UI** = 方向盘和仪表盘（用户交互界面）

---

## 技术选型对比

### Python 运行环境

| 维度 | Termux 版 | Chaquopy 版 |
|------|-----------|-------------|
| **Python 类型** | 完整的 Linux Python 3.11 | 嵌入式 Android Python 3.8-3.11 |
| **安装方式** | 用户手动安装 (`pkg install python`) | 开发时打包进 APK |
| **库支持** | 几乎所有库（pip install 任意包） | 纯 Python 库 ✅，C 扩展库部分支持 ⚠️ |
| **环境隔离** | 独立进程，完全隔离 | 嵌入 APK，共享内存空间 |
| **升级方式** | 用户可自行升级 | 需要重新发布 APK |

**通俗理解**：
- **Termux** = 微型 Docker 容器（完整 Linux 环境）
- **Chaquopy** = 静态链接库（编译进可执行文件）

---

### 通信机制

#### Termux 版：跨进程 HTTP 通信
```python
# Python 在 Termux 进程里
import requests
response = requests.post("http://localhost:8080/tap", json={"x": 500, "y": 500})
```

```kotlin
// Kotlin 在 Helper App 进程里
class HttpServer {
    fun handleTap(x: Int, y: Int) {
        performGesture(x, y)  // 执行点击
    }
}
```

**特点**：
- ✅ 解耦彻底（两个进程互不影响）
- ❌ 有网络开销（虽然是 localhost）
- ❌ 需要两个 App 都保活

---

#### Chaquopy 版：进程内直接调用
```python
# Python 在 APK 内部
import android_helper
android_helper.click(500, 500)  # 直接调用 Kotlin 函数
```

```kotlin
// Kotlin 在同一个 APK 里
class AndroidHelper {
    @JvmStatic
    fun click(x: Int, y: Int) {
        performGesture(x, y)
    }
}
```

**特点**：
- ✅ 调用速度快（无网络开销）
- ✅ 只需一个 App
- ⚠️ 耦合度高（Python 挂了可能影响 App）

**注**：我们当前实现为了复用代码，仍保留了 HTTP 层，但这是可选的。

---

## 用户体验对比

### 安装部署

#### Termux 版
```bash
# 步骤 1：安装 Termux
从 F-Droid 下载安装

# 步骤 2：配置 Python 环境
pkg update
pkg install python
pip install requests pillow openai

# 步骤 3：下载脚本
git clone https://github.com/xxx/AutoGLM-Hybrid
cd AutoGLM-Hybrid/termux-scripts

# 步骤 4：安装 Helper APK
安装 autoglm-helper.apk

# 步骤 5：开启权限
在系统设置中开启无障碍服务

# 步骤 6：运行
python agent_main.py --task "打开淘宝"
```

**用户门槛**：⭐⭐⭐⭐⭐（需要懂命令行）

---

#### Chaquopy 版
```bash
# 步骤 1：安装 APK
下载 autoglm-helper.apk 并安装

# 步骤 2：开启权限
打开 App，点击"检查无障碍权限"

# 步骤 3：使用
输入任务 -> 点击"开始执行"
```

**用户门槛**：⭐（普通用户零门槛）

---

### 运行时体验

| 场景 | Termux 版 | Chaquopy 版 |
|------|-----------|-------------|
| **启动方式** | 打开 Termux，敲命令 | 打开 App，点按钮 |
| **任务输入** | 命令行参数 | 图形界面输入框 |
| **日志查看** | 终端输出 | App 内日志窗口 |
| **后台保活** | 需要保活两个 App | 只需保活一个 App |
| **崩溃恢复** | Python 崩溃不影响 Helper | Python 崩溃可能影响 App |
| **更新升级** | 用户自行 `git pull` | 重新下载 APK |

---

## 开发维护对比

### 开发复杂度

#### Termux 版
```
开发工作量：
├── Android App 开发      ⭐⭐⭐
├── Python 脚本开发       ⭐⭐
├── 部署脚本编写          ⭐⭐⭐
├── 用户文档编写          ⭐⭐⭐⭐⭐
└── 用户支持成本          ⭐⭐⭐⭐⭐
```

**痛点**：
- 用户环境千差万别（Python 版本、库版本）
- 调试困难（需要用户提供 Termux 日志）
- 文档需要非常详细（命令行步骤）

---

#### Chaquopy 版
```
开发工作量：
├── Android App 开发      ⭐⭐⭐⭐
├── Python 脚本开发       ⭐⭐
├── Chaquopy 集成配置     ⭐⭐⭐
├── 用户文档编写          ⭐
└── 用户支持成本          ⭐⭐
```

**优势**：
- 环境统一（所有用户的 Python 版本一致）
- 调试简单（APK 日志直接查看）
- 文档简洁（安装即用）

---

### APK 体积对比

| 版本 | APK 大小 | 说明 |
|------|----------|------|
| Termux 版 Helper | ~5 MB | 只包含 Android 代码 |
| Chaquopy 版 | ~30-50 MB | 包含 Python 解释器 + 常用库 |

**权衡**：
- Termux 版：APK 小，但用户需要额外下载 Termux (~100MB) + Python 环境
- Chaquopy 版：APK 大，但一次安装全搞定

---

## 适用场景

### Termux 版适合：

✅ **开发者/极客用户**
- 熟悉命令行
- 需要频繁修改 Python 代码
- 需要使用特殊的 Python 库

✅ **实验性项目**
- 快速原型验证
- 不需要考虑用户体验

✅ **多设备控制**
- 一台电脑控制多台手机
- 需要集中管理

---

### Chaquopy 版适合：

✅ **普通用户**
- 不懂编程
- 只想"开箱即用"
- 需要稳定可靠

✅ **商业产品**
- 需要发布到应用商店
- 需要统一的用户体验
- 需要降低支持成本

✅ **单机自动化**
- 手机自己控制自己
- 不需要外部设备

---

## 总结

### 一句话概括

| 版本 | 定位 |
|------|------|
| **Termux 版** | 极客的瑞士军刀（灵活但复杂） |
| **Chaquopy 版** | 普通人的智能助手（简单但强大） |

### 技术类比

| 版本 | 类比 |
|------|------|
| **Termux 版** | Docker 容器 + 微服务架构 |
| **Chaquopy 版** | 单体应用 + 静态链接 |

### 我们为什么选择 Chaquopy？

因为我们的目标是：
> **让每个人都能用上 AI 自动化，而不是只有程序员能玩。**

我们相信：
- 技术应该服务于人，而不是让人适应技术。
- 最好的产品是"用户感觉不到技术存在"的产品。
- 降低门槛，才能让 AI 真正普及。

---

## 附录：核心代码对比

### Termux 版：android_helper.py
```python
import requests

def click(x, y):
    """通过 HTTP 请求控制 Helper App"""
    response = requests.post(
        "http://localhost:8080/tap",
        json={"x": x, "y": y}
    )
    return response.json()["success"]
```

### Chaquopy 版：android_helper.py
```python
import requests  # 仍然使用 HTTP（为了复用代码）

def click(x, y):
    """通过 HTTP 请求控制同一个 App 内的服务"""
    response = requests.post(
        "http://localhost:8080/tap",
        json={"x": x, "y": y}
    )
    return response.json()["success"]

# 也可以直接调用（更高效）：
# from com.autoglm.helper import AutoGLMAccessibilityService
# AutoGLMAccessibilityService.getInstance().performTap(x, y)
```

**关键点**：两个版本的 Python 代码几乎一样！这就是架构设计的巧妙之处。

---

**文档版本**：v1.0  
**最后更新**：2025-12-18  
**作者**：AutoGLM 开发团队
